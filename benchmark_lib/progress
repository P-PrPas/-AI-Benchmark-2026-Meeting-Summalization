#!/usr/bin/env python3
"""
benchmark_lib/progress  — Mock script for LOCAL TESTING only
Server จะ mount ของจริงมาแทนตอน inference จริง

Usage: /benchmark_lib/progress <n>
"""
import sys

n = sys.argv[1] if len(sys.argv) > 1 else "?"
print(f"[benchmark_lib/progress] ✅ progress signal: {n}", flush=True)
